# SPY / QQQ 定投提醒策略规格说明

本文档记录当前项目已实现的 SPY / QQQ 定投提醒策略、数据口径、消息格式和运行约束。

---

## 1. 运行方式

- GitHub Actions workflow 只保留 `workflow_dispatch`。
- 外部 cron-job 每 5 分钟触发 workflow。
- workflow 安装项目依赖后运行：

```text
python -m dca_reminder.main
```

- 程序内部使用 `America/New_York` 判断 NYSE 交易日和有效窗口。
- 常规盘中判断窗口为 `09:30 <= 美东时间 <= 16:00`。
- 收盘总结窗口为 `美东时间 >= 16:15`，用于等待 yfinance 返回准确日线收盘价。

---

## 2. 支持标的与参数

SPY 和 QQQ 独立判断、独立触发、独立记录状态、独立发送 Telegram 消息。

| 标的 | 单日下跌阈值 | 单月下跌阈值 | 20MA 偏离阈值 | 50MA 偏离阈值 |
|---|---:|---:|---:|---:|
| SPY | 1.5% | 5.0% | 8.0% | 12.0% |
| QQQ | 2.0% | 7.0% | 12.0% | 16.0% |

---

## 3. 数据口径

- 使用 yfinance 免费行情源。
- 历史日线使用拆股复权、不分红复权的 `Close` 口径。
- 不使用 `Adj Close` 作为触发基准，避免分红造成误判。
- 盘中价格使用 yfinance `1d / 5m` 最新有效 `Close`，失败时再尝试 `fast_info`。
- MA20、MA50 使用同一日线 `Close` 口径计算。
- 近 30 日涨跌幅按自然日向前 30 天取基准；如果 30 天前不是交易日，则向前找最近一根日线收盘价。

### 3.1 上一交易日收盘价校验

单日下跌的初始基准必须来自 NYSE 日历上的上一实际交易日。

程序会先用 NYSE 日历算出上一实际交易日，再要求 yfinance 日线中必须存在该日期的有效 `Close`。

如果 yfinance 当轮只返回更早日期，例如缺少上周五收盘价，则本轮跳过该标的并写日志，不使用更早一天的收盘价兜底。

### 3.2 收盘数据有效性

每日、每周、每月总结只在以下数据均为有效有限数值时发送：

```text
1. 当日收盘价；
2. 收盘口径 MA20；
3. 收盘口径 MA50。
```

如果 yfinance 返回 `NaN`、`inf` 或缺失值，则不发送总结、不写入已发送状态，等待下一轮 workflow 重试。

---

## 4. 每周基础提醒

每周第一个实际交易日，每个标的发送一次基础提醒。

- 优先在开盘后发送。
- 如果 GitHub Actions 或 cron-job 延迟，只要当天仍是本周第一个交易日且处于交易时段内，可以补发一次。
- 每周基础提醒参与每日、每周、每月统计。

---

## 5. 单日下跌阶梯

### 5.1 初始基准

每个交易日的单日下跌初始基准为：

```text
NYSE 上一实际交易日收盘价
```

### 5.2 首次触发

```text
SPY 当前价 <= 上一实际交易日收盘价 * 98.5%
QQQ 当前价 <= 上一实际交易日收盘价 * 98.0%
```

### 5.3 同日阶梯再触发

同一交易日内，如果已经触发过单日下跌提醒，下一阶梯以上一次单日触发价为基准：

```text
SPY 当前价 <= 上一次单日触发价 * 98.5%
QQQ 当前价 <= 上一次单日触发价 * 98.0%
```

单日阶梯价仅当日有效，下一交易日重置。

---

## 6. 单月下跌阶梯

### 6.1 初始基准

每个自然月的单月下跌初始基准为：

```text
上月最后一个交易日收盘价
```

### 6.2 首次触发

```text
SPY 当前价 <= 上月最后交易日收盘价 * 95.0%
QQQ 当前价 <= 上月最后交易日收盘价 * 93.0%
```

### 6.3 月内阶梯再触发

同一自然月内，如果已经触发过单月下跌提醒，下一阶梯以上一次单月触发价为基准：

```text
SPY 当前价 <= 上一次单月触发价 * 95.0%
QQQ 当前价 <= 上一次单月触发价 * 93.0%
```

单月阶梯价在本自然月内持续有效，新自然月重置。

---

## 7. 均线偏离提醒

20MA 和 50MA 独立判断，且每个自然月每标的最多触发一次。

```text
SPY 20MA：当前价 <= MA20 * 92.0%
SPY 50MA：当前价 <= MA50 * 88.0%
QQQ 20MA：当前价 <= MA20 * 88.0%
QQQ 50MA：当前价 <= MA50 * 84.0%
```

---

## 8. 多条件提醒

同一标的同一轮运行中多个条件同时新触发时，只发送一条盘中提醒。

消息按以下优先级列出条件：

```text
50MA 偏离
20MA 偏离
单月下跌
单日下跌
每周基础
```

---

## 9. 收盘确认与状态补录

每日总结会区分：

```text
1. 盘中触发；
2. 收盘确认。
```

如果盘中没有触发，但收盘价确认已经达到单日、单月或均线条件，程序会将收盘确认补录到状态中：

- 不写入 `intraday_signals`，避免误显示成盘中触发。
- 写入 `close_confirmed_signals`。
- 同步更新当日阶梯价、周统计、月统计和触发记录。

---

## 10. Telegram 消息字段

所有消息使用双时区时间：

```text
美东时间：YYYY-MM-DD HH:MM
北京时间：YYYY-MM-DD HH:MM
```

涨跌幅使用短格式，减少 Telegram 自动换行：

```text
当日：+1.50%（基准100.00）
当月：-3.33%（基准105.00）
30日：+3.57%（基准98.00）
```

盘中触发消息会在触发次数后展示阶梯阈值历史，例如：

```text
单日下跌提醒：第 2 次（-2.0%，-2.0%）
```

---

## 11. 总结消息

### 11.1 每日总结

每日总结在 `16:15 ET` 后首次拿到有效收盘数据时发送。

包含：

```text
1. 收盘价；
2. 当日、当月、30日涨跌幅和基准价；
3. 盘中触发统计；
4. 收盘确认统计；
5. 明日单日初始触发价；
6. 本月月跌下一阶梯价。
```

### 11.2 每周总结

每周总结在本周最后一个实际交易日每日总结后发送一次。

包含：

```text
1. 本周收盘价；
2. 当日、当月、30日涨跌幅和基准价；
3. 本周各类提醒统计；
4. 下周单日初始触发价；
5. 本月月跌下一阶梯价。
```

### 11.3 每月总结

每月总结在本月最后一个实际交易日每日总结后发送一次。

包含：

```text
1. 月末收盘价；
2. 当月、30日涨跌幅和基准价；
3. 本月各类提醒统计；
4. 下月月跌初始基准；
5. 下月第一次月跌触发价。
```

---

## 12. 状态文件

状态保存在仓库内非敏感文件：

```text
data/state.json
```

状态按标的独立维护：

```text
symbols.<symbol>.days
symbols.<symbol>.weeks
symbols.<symbol>.months
```

GitHub Actions 只在状态变化时提交 `data/state.json`。

---

## 13. 诊断日志

每轮盘中和收盘路径都会输出非敏感诊断日志：

```text
current_price
previous_close
daily_trigger_line
daily_change
daily_triggered
```

这些日志用于排查“价格看起来触发但没有提醒”或“触发基准不符合预期”的问题，不包含 Telegram token 或 chat id。

---

## 14. 本地验证

```powershell
.\.venv\Scripts\python.exe -m pytest
```

dry-run 运行：

```powershell
$env:DRY_RUN = "1"
.\.venv\Scripts\python.exe -m dca_reminder.main
```
